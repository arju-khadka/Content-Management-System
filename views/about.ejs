<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta http-equiv="refresh" content="2 ">
  <title>About Us</title>

  <style>
    :root {
      --green: #005C32;
      --gold: #D3AF5E;
    }

    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: #f6f6f6;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* NAVBAR */
    .nav {
      background: var(--green);
      padding: 1rem;
      color: #fff;
    }
    .nav .container {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .brand {
      font-size: 1.3rem;
      font-weight: bold;
    }
    .nav-links {
      list-style: none;
      display: flex;
      gap: 1rem;
      margin: 0;
      padding: 0;
    }
    .nav-links a {
      color: #fff;
      text-decoration: none;
      font-weight: 600;
    }

    /* ABOUT SECTION */
    .about-section {
      flex: 1;
      padding: 3rem 2rem;
      max-width: 900px;
      margin: auto;
      background: #ffffff;
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    }
    .about-section h1 {
      margin-top: 0;
      color: var(--green);
      font-size: 2rem;
    }
    .about-section p {
      line-height: 1.6;
      color: #333;
    }

    /* FOOTER */
    footer {
      background: var(--green);
      color: #fff;
      text-align: center;
      padding: 1rem;
      margin-top: 2rem;
    }
  </style>
</head>

<body>
  <%- include("navbar.ejs") %>

  <section class="about-section">
    <h1>About Us</h1>
    <p>
      Welcome to our website! We are dedicated to providing top-quality services and exceptional
      user experience. Our mission is to make your online journey smooth, enjoyable, and productive.
    </p>
    
  </section>

   <%- include("footer.ejs") %>

</body>
</html>