<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Content</title>

    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        /* SAME NAVBAR AS HOMEPAGE */
        nav{
            width: 100%;
            background: #005C32;
            padding: 15px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        nav .logo{
            color: white;
            font-size: 22px;
            font-weight: bold;
        }

        nav ul{
            display: flex;
            gap: 25px;
            list-style: none;
        }

        nav ul li a{
            color: #D3AF5E;
            text-decoration: none;
            font-weight: bold;
            font-size: 16px;
        }

        nav ul li a:hover{
            color: white;
        }

        /* PAGE BODY */
        .container{
            width: 100%;
            height: calc(100vh - 140px); /* header + footer height */
            display: flex;
            justify-content: center;
            align-items: center;
            background: #f5f5f5;
        }

        .form-box{
            width: 420px;
            background: white;
            padding: 25px;
            border-radius: 10px;
            border-top: 6px solid #005C32;
            box-shadow: 0px 0px 15px rgba(0,0,0,0.1);
        }

        h2{
            color: #005C32;
            text-align: center;
            margin-bottom: 20px;
        }

        label{
            display: block;
            margin-bottom: 5px;
            color: #005C32;
            font-weight: bold;
        }

        input, textarea{
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #D3AF5E;
            border-radius: 5px;
        }

        textarea{
            height: 100px;
        }

        button{
            width: 100%;
            padding: 12px;
            background: #005C32;
            color: white;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover{
            background: #003d22;
        }

        /* SAME FOOTER AS HOMEPAGE */
        footer{
            background: #005C32;
            color: white;
            text-align: center;
            padding: 15px 0;
            margin-top: 0;
        }
    </style>
</head>
<body>

    <!-- SAME NAVBAR -->
    <nav>
        <div class="logo">CMS Project</div>
        <ul>
            <li><a href="home.html">Home</a></li>
            <li><a href="about.html">About</a></li>
            <li><a href="create.html">Create</a></li>
        </ul>
    </nav>

    <!-- FORM SECTION -->
    <div class="container">
        <div class="form-box">
            <h2>Create New Content</h2>

            <form action="/update/<%-id%> " method="post">
                <label>Update Title</label>
                <input type="text" value="<%- blog.title%>" name="title" placeholder="Enter title" required>

                <label>Update SubTitle</label>
                <input type="text" value="<%- blog.subTitle%>" name="subTitle" placeholder="Enter subtitle" required>

                <label>Update Description</label>
                <textarea name="description" placeholder="Enter description" required><%- blog.description %></textarea>

                <button type="submit">Submit</button>
            </form>
        </div>
    </div>

    <!-- SAME FOOTER -->
    <footer>
        © 2025 CMS Project | All Rights Reserved
    </footer>

</body>
</html>
